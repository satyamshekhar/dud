(defun scaligent-post-checkout () 
  "Runs post-checkout"
  (interactive)
  ())

(defun scaligent-compile ()
  "Compile everything"
  (interactive)
  ())

(defun scaligent-push-for-review ()
  "Pushes current branch for review"
  (interactive)
  ())

(defun scaligent-submit ()
  "Submits the current commit"
  (interactive)
  ())

(defun scaligent-download-comments ()
  "Download comments from gerrit and displays them."
  (interactive)
  ())

(defun scaligent-start-single-node ()
  "Runs start single node"
  (interactive)
  ())

(defun scaligent-start-local-cluster ()
  "Starts local cluster"
  (interactive)
  ())

(defun scaligent-start-tomcat ()
  "Starts tomcat"
  (interactive)
  ())

(defun scaligent-start-nginx ()
  "Starts nginx"
  (interactive)
  ())

(defun scaligent-start-falcon ()
  "Starts falcon"
  (interactive)
  ())

(defun scaligent-start-sage ()
  "Starts sage"
  (interactive)
  ())

(defun scaligent-test-callosum ()
  "Runs test for callosum"
  (interactive)
  ())

(defun scaligent-test-all ()
  "Runs all test"
  (interactive)
  ())

(defun scaligent-test-falcon ()
  "Runs test for falcon"
  (interactive)
  ())

(defun scaligent-test-sage ()
  "Runs test for sage"
  (interactive)
  ())

(defun scaligent-lint ()
  "Runs lint on modified files"
  (interactive)
  ())

(defun scaligent-log-dir ()
  "Opens logs directory"
  (interactive)
  ())

(defun scaligent-root-dir ()
  "Opens scaligent root dir"
  (interactive)
  ())

(defun scaligent-toolchain-dir ()
  "Opens scaligent toolchain dir"
  (interactive) 
  ())

(defun scaligent-nginx-config ()
  "Opens nginx config file."
  (interactive)
  ())

(provide 'scalemacs)
